Types  of Constructor
	Private Constructor
	Public Constructor 
	Internal Constructor 
	Protected Constructor 
	Static constructor

Private constructor : 
	->supports in object creation where we have to emphasize on singleton object

Public Constructor :

Internal Constructor :

Protected Constructor : 
	->like a private constructor only meant for the class which is defined and all the sub classes

Static constructor : 
	->are used to initialize static fields as soon as the class is refernces the first time where  as an instance constructor is used to create an instance with the new keyword 
	->does not take any modifiers nor any parameters
	->this will be called first after that instance is called
	->only one time can be executed in a class
	    eg : Log file entries

``````````````````````````````````````````````````````

Program :

class Accounts{
	public int id;
	public string name;
	public static float rateofinterst

	// instance constructor pr normal
	public Account(int id, string name){
		this.id = id;
		this.name = name;
		rateofinterest = 0.5f;
	}

	//static constructor
	static Account(){
		rateofinterest = 0.7f;
	}
	public void Display(){
		Write(id + name + rateofinterest
	}
}

class Dog{
	public string name;
	public int age;

	//protected constructor
	protected Dog(){
		Write("Protecting DOg");
		Write (name + age);
	}
}
class Labrador : Dog{
	public double measurements;
	public Labrador(string name , int age, double measurements){
		Name = name;
		Age = age;
		this.measurements = measurements
		Write(name + age + measurements);
	}
}

class ConstructorTypes{
	static void Main(){
		Account a1 = new Account(101, "Karthi");
		Account a2 = new Account(102,"Arun");
		a1.Display();
		a2.Display();
		Write("Protected);
		Labrador lb = new Labrador("juffy", 5, 12.54);
		Console.Read();
	}
}
