			interface			Abstract Class


Multiple Inheritance	A class may inherit several	A class can inherit only one abstract class 
			interfaces

Default implementation	No code Only signature 		complete / default code/ or just signature

Access Modifiers 	No access modifier, all are	can contain access modifiers(func,prop etc) 
			public

Core vs Peripheral	Are used to define peripheral	used to define core identity for a class and used by objects of same type 
			abilities

Speed 			Requires more time to find     	fast 
			the actual method

Fields adn Constants 	NO 				yes

Adding functionality 	involves tracing down all 	have options to provide default implementation
			the implementation

Types of Class : 
  1. Abstract Class - cannot be instantiated, but can be inherited
  2. Static class - neither be inherited nor be instantiated, all members have to be static only. Is called using the classname
  3. Sealed Class - Can be instantiated, but cannot be inherited 

sealed class FinalClass{
	public void sealedFunc(){
		Write("Sealed ");
	}
	Main{
		finalClass fc = new FInalClass();
		fc.sealedFunc();	
	}
}

  4.Partial Class - Can be defined in multiple files since multiple developers work simultaneously

partial class Marks{
	public void SampleFunc(){
		write("Hi Partial");
	}
}