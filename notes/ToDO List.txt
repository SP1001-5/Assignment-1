ToDO List:

using Collections;
Class ToDoList{
	public string Task{get; set;}
	public string Priority {get; set;}
	
	ArrayList todoArray = new ArrayList();	

	//adding tasks with priority into arrayList

	public void AddTask(string usertask, string userpriority){
		todoArray.Add(new ToDoList{ Task = usertask, Priority = userpriority});
	}
	
	//Show the task with priority

	public ArrayList DisplayingToDoList(){
		return todoArray;
	}

	//Remove task from the arraylist

	public void RemoveTask(string usertask){
		foreach(ToDoList item in todoArray)
		{
			if(item.Task == usertask){
			todoArray.Remove(item);
			}
		}
	}
}
class CHeckToDo{
	static void Main(){
		ToDoList tdList = new ToDoLiat();
		int choice;
		do{
			Write("Enter your choice 1. ADD   2.Display   3.Remove");
			Choice = Convertint();
			switch(choice){

			case 1:
			tdList.AddTask("Meet,2);
			tdList.AddTask("Visit,1);
			break;

			case 2;
			ArrayList dList = tdList.DisplayingToDoList
			foreach(ToDoList item in dList){
				write(item.task);
				write(item.priority
			}
			break;
			
			case 3:
			tdList.RemoveTask("Visit");
			Write("Removed success");
			break;
			default:
			break;
			}
		}
		while(choice<=3)
	}	
}