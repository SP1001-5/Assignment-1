internal class TypeConversion{
	static void Main{
		TypeConversion typec = new TypeConversion();
		typec.TypeChange();
		typec.Try_Parsing();
		typec.Boxing();
		Others others = new Others();
		int res = others.Addnos(4,6);
		Console.WriteLine(res);
		Others.Ternary_func();
		Console.Read()



	//value type to refernce type is called boxing

	public void Boxing(){
		int i =10
		object obj; //refernce 
		obj = i   //value to refernce
		string s = "AAAA"
		obj = s   //implicit casting

	//reference type to value is unboxing
		float salary;
		salary = float.Parse(console.ReadLine());
		console.WriteLine(salary);
		bool b;
		b = convert.ToBoolean(console.ReadLine());

	}

	public void Try_Parsing(){
		int n =100;
		float f =23456789.067f;
		n = int.TryParse(f);

		string str = "1000g";
		int i = int.Parse(str); //exception
		Console.WriteLine(I);
	}

->parse will provide an exception 
->try parse will provide Boolean with return type(output) if parse happens
->try parse will not give exception

		string str = "1000g"
		int result = 0;
		bool success = int TryParse(str, out result);
		if(success){
			Console.WriteLine(result);
		}
		else{
			Wrie("Inavalid");
		}

class Others{
	public static void Ternary_func(){
		int num = 10;
		bool b;
		b = num == 10 ? true:false;
		COnsole.WriteLine(b);
	}
	static void NullConcept(){
		int? number = null; //nullable value type
		int sum;
		//converting null int number to a int sum
		sum = (int) number  // or
		sum = number.Value;
		sum = number1 ?? 0; //null coalescing operator
		WriteLine(sum) //output: 0
	}
	public int Addnos(int a,int b){
		return a+b;
	}
}
	

->null cannot be given to value type
->if else can be replaced with null coalescing operator(??)