Structures: are user defined value types
	    different data types stored in same place like arrays.
	    it is a data type whiie class is a reference type

Enumeration: collection of named constants
 	     user defined value types	
	     default type of any enumeration list is int 
	     is used to store a list of values that generally correspond to a particular type
	    
	
namespace day2{

	Enum Cities{Bangalore=2, Agra=3, Chennai=5, Delhi=1}

	

	struct Student{
		public int id;
		public float mathsmark
		public string name;

		public void ShowStudent(){
			WriteLine($"Id: {id}, Name:{name}, maths mark: {mathsmark}");
		}
	}
internal class UserdefinedValueTypes{

	public static void Understandenums{

		foreach (int item in Enum.Getvalues(typeof(Cities))){
			WriteLine(item);
			if (item==2)
				Write(Enum.GetName(typeof(Cities),item) + "is a garden city")
		}

		foreach( var name in Enum.GetNames(typeof(Cities))){
			Write(var);
		}
	}

	public static void Main(){
		Student s1 = new Student();
		s1.id = 1;
		s1.name = "Kumar";
		s1.Mathsmark=78;
		s1.showStudent();

		Student s2=s1;
		s2.ShowStudent();
		UNderstandenums();
		Console.Read();
	}
}
}    