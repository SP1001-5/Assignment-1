Generics
	are a way to write classes/Interfaces/ Methods/Properties/Indexers etc.. decoupled with datatypes
	is a type safe mechanism to define the various elements without compromising
	  -type safety
	  -Perfomance
	  -Productivity
	
Common Generic Classes:

Collection 			generics

ArrayList			List
HashTable 			Dictioanry
SortedList			SortedList
Stack				Stack
Queue				Queue

Class Program{
	public static void ListOps1(){
		List<int> list1 = new List<int>();
		list1.Add(12);
		list1.Add(1);
		list1.Add(2);
		list1.Add(3);
		
		list1.Sort();
		foreach(int i in list1){
			Write(i);
		}
	}

	public static void ListOps2(){
		List<EMployee> emplist = new List<Employee>();
		emplist.Add(new Employee(101, "Kamal", 13000, "Infinite");
		emplist.Add(new Employee(101, "Kamal", 11000, "Infinite");
		emplist.Add(new Employee(101, "Kamal", 14000, "Infinite");
		emplist.Add(new Employee(101, "Kamal", 15000, "Infinite");
		
		foreach(Employee emp in emplist){
			Write(emp.ToString());
		}

		Write ("After sorting on salary");
		emplist.Sort();
		foreach(Employee emp in emplist){
			Write(emp.ToString());
		}
	}

	Static void Main(string[] args){
		ListOps1();
		ListOps2();
	}	
}

//comparing objects in a list using CompareTo of Icomparable

class Employee : Icomparable{
	int Empid; 
	string EmpName;
	float EmpSalary;
	string ComapnyName;

	public Employee(int Empid,string EmpName,float EmpSalary,string ComapnyName){
		Empid = empid;
		EmpName = empname;
		EmpSalary = empsalary;
		CompanyName = companyname; 
	}
	
	public int CompareTo(Object objr){
		Employee other = obj as Employee;
		if(this.EmpSalary == other.EmpSalary){
			return this.Empid.ComapreTo(other.Empid);
		}
		//return other.EmpSalary.ComapreTo(other.Empid);  //desc
		return this.EmpSalary.CompareTo(other.EmpSalary); //Ascen

	}
	
	public override string ToString(){
		return string.Format(Empid + EmpName + EmpSalary + CompanaName);
	}	
}